import { TimeSlotToTimePipe } from './time-slot-to-time.pipe';

describe('TimeSlotToTimePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeSlotToTimePipe();
    expect(pipe).toBeTruthy();
  });
});
