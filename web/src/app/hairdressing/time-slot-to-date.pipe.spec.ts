import { TimeSlotToDatePipe } from './time-slot-to-date.pipe';

describe('TimeSlotToDatePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeSlotToDatePipe();
    expect(pipe).toBeTruthy();
  });
});
