import { ReportListTreeBuilderPipe } from './report-list-tree-builder.pipe';

describe('ReportListTreeBuilderPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportListTreeBuilderPipe();
    expect(pipe).toBeTruthy();
  });
});
