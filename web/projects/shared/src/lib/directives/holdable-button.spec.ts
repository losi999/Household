import { HoldableButton } from './holdable-button';

describe.todo('HoldableButton', () => {
  it('should create an instance', () => {
    const directive = new HoldableButton();
    expect(directive).toBeTruthy();
  });
});
